{% extends 'base.html.twig' %}

{% block title %}{{ title }} - Mon Blog{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>

    {{ form_start(form, {'attr': {'class': 'mt-3'}}) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary mt-3">Enregistrer</button>
        <a href="{{ path('app_admin_categories') }}" class="btn btn-secondary mt-3">Annuler</a>
    {{ form_end(form) }}
{% endblock %}
